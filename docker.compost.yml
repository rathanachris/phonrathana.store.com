version: '3.8'

services:
  backend:
    build: .
    container_name: java_backend
    ports:
      - "8081:8080"
    depends_on:
      - db
    networks:
      - my_network

  db:
    image: mysql:8
    container_name: www.phontathana.stoer.com_sql_db
    environment:
      MYSQL_ROOT_PASSWORD: root@phonrathana.s.pass
      MYSQL_DATABASE: phonrathana.stoer.com_db
      MYSQL_USER: christathana_user
      MYSQL_PASSWORD: secret.
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
    networks:
      - my_network 

  webapp:
    image: nginx:latest
    container_name: phonrathana.stoer.com_web_app
    ports:
      - "8080:80"
    volumes:
      - ./webapp:/usr/share/nginx/html
    networks:
      - my_network

networks:
  my_network:
    driver: bridgeo
